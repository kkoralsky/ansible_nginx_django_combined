galaxy_info:
  author: Karol Ossowski
  description: nginx config for Django (uwsgi proxy & static server)
  license: BSD

  min_ansible_version: 1.2

  platforms:
  - name: Debian
    versions:
    - all

  galaxy_tags: [nginx, uwsgi, django, static]

dependencies:
  - role: nginx_base
